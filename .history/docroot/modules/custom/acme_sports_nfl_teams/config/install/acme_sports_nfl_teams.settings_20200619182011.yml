# Normally sensitive credentials such as these would be pulled in
# through the key module and stored in settings.php as environment variables.
base_url: 'http://delivery.chalk247.com/team_list/NFL.JSON'
api_key: '74db8efa2a6db279393b433d97c2bc843f8e32b0'  
dependencies:
  enforced:
    module:
      - acme_sports_nfl_teams
